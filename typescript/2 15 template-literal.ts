(function () {
    function getEdad() {
        return 100 + 200;
    };
    const nombre = 'Fernando';
    const apellido = 'Herrera';
    const edad = 30

    const salida =`${nombre} ${apellido} ${getEdad()}`;
    console.log(salida); 
})();

 